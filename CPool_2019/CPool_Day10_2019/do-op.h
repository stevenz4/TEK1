/*
** EPITECH PROJECT, 2019
** do-op
** File description:
** include
*/

#define OPPLUS(x,y)  ((x) + (y))
#define OPMINUS(x,y)  ((x) - (y))
#define OPMULT(x,y)  ((x) * (y))
#define OPDIV(x,y)  ((x) / (y))
#define OPMOD(x,y) ((x) % (y))
